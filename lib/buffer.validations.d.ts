/** Native buffers use u32 cell offsets and signed drawing coordinates. */
export declare function validateBufferDimensions(width: number, height: number): void;
export declare function validateGrayscaleSource(intensities: Float32Array, width: number, height: number): void;
