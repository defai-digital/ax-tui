/**
 * Bun-only SolidJS transform preload for ax-tui.
 *
 * Importing this entrypoint from Node.js throws. Use Bun so the transform
 * plugin is installed automatically before application modules load.
 *
 * @module
 */
export {}
